# The storyteller contract

Input context and output JSON schema (narration, npc_dialogue, quest_changes, world_changes, rumors, chronicle_entry, memory_updates), versioning, validation rules, and what the AI may never do. Stub.
