import assert from 'node:assert/strict'; import test from 'node:test'; import { EncounterSystem, RumorSystem, SeededRng, spreadRumor } from '../../../packages/engine/src/index.js'; import { TRAVELER_IN_TROUBLE } from '../../../packages/game/src/content/encounters/templates.js';
test('rumors preserve truth, exaggeration, and false variants from one event', () => { const system = new RumorSystem(); const rumors = system.fromEvent({ id: 'beast_slain', actor: 'Eldric' }, { true: '{actor} slew the beast with a sword.', exaggerated: '{actor} slew it barehanded.', false: 'The beast still walks.' }); assert.deepEqual(rumors.map((r) => r.truth), ['true','exaggerated','false']); const spread = spreadRumor(rumors[0], 'ashford', new SeededRng(2)); assert.equal(spread.settlement, 'ashford'); });
test('AI variables can only fill authored encounter templates', () => { const system = new EncounterSystem(); system.register(TRAVELER_IN_TROUBLE); const encounter = system.create('traveler_in_trouble', { traveler: 'herbalist', enemy: 'wolf', reason: 'medicine', reward: 'tonic', complication: 'traveler_lied' }); assert.equal(encounter.spawns[0].count, 3); assert.throws(() => system.create('traveler_in_trouble', { enemy: 'dragon' }), /Unsupported/); });
