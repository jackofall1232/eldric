export class LightingModel { constructor(budget = 12) { this.budget = budget; this.lights = []; this.darkness = 0; } setDarkness(value) { this.darkness = Math.max(0, Math.min(.85, value)); } add(light) { if (this.lights.length >= this.budget) return false; this.lights.push({ color: '#e38b45', radius: 48, intensity: 1, ...light }); return true; } clear() { this.lights.length = 0; } visible(camera) { return this.lights.filter((light) => camera.isVisible(light.x, light.y, light.radius, light.radius)); } }
