export function assembleChapters(entries, maximumPerChapter = 6) { const chapters = []; for (let index = 0; index < entries.length; index += maximumPerChapter) { const page = entries.slice(index, index + maximumPerChapter); chapters.push(Object.freeze({ number: chapters.length + 1, title: page.find((entry) => entry.category === 'place')?.title || page[0]?.title || `Chapter ${chapters.length + 1}`, entries: page, categories: [...new Set(page.map((entry) => entry.category))] })); } return chapters; }
