export class Stamina { constructor(maximum = 100, regeneration = 24) { this.maximum = maximum; this.value = maximum; this.regeneration = regeneration; this.delay = 0; } spend(cost, delay = .35) { if (this.value < cost) return false; this.value -= cost; this.delay = Math.max(this.delay, delay); return true; } update(delta) { this.delay = Math.max(0, this.delay - delta); if (!this.delay) this.value = Math.min(this.maximum, this.value + this.regeneration * delta); } ratio() { return this.value / this.maximum; } }
